function task(name, dueDate) {
  const dated = '';
  const status = 'new';
  const progress = 'pending';

  function rename(newName) {

  }

  function remove() {

  }

  return {
    name,
    dueDate,
    dated,
    status,
    progress,
    rename,
    remove,
  };
}
