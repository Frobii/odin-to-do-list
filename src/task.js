const task = () => {
  const properties = {
    name: '',
    dated: '',
    dueDate: '',
    status: 'New',
    progress: 'In Progress',
    description: '',
  };

  return {
    properties,
  };
};

export default task;
